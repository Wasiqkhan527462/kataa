<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Transaction Form</title>
  <style>
    .transaction-form {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ddd;
      border-radius: 8px;
      background-color: #f9f9f9;
    }

    .form-group {
      margin-bottom: 15px;
    }

    .form-label {
      display: block;
      font-size: 14px;
      font-weight: bold;
      margin-bottom: 5px;
      color: #333;
    }

    .form-input {
      width: 100%;
      padding: 10px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }

    .form-input:focus {
      border-color: #007bff;
      outline: none;
    }

    .form-submit {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
      width: 100%;
    }

    .form-submit:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
<%= form_with model: transaction, html: { class: "transaction-form" } do |form| %>
  <div class="form-container">
    
    <div class="form-group">
      <%= form.label :customer_id, "Customer", class: "form-label" %>
      <%= form.collection_select :customer_id, Customer.all, :id, :name, { prompt: "Select a customer" }, class: "form-input" %>
    </div>
    
    <div class="form-group">
      <%= form.label :transaction_type, class: "form-label" %>
      <%= form.select :transaction_type, [["Credit", "credit"], ["Debit", "debit"]], {}, class: "form-input" %>
    </div>
    
    <div class="form-group">
      <%= form.label :date, class: "form-label" %>
      <%= form.date_field :date, value: Date.today, class: "form-input" %>
    </div>

    <div class="form-group">
      <%= form.label :time, class: "form-label" %>
      <%= form.time_field :time, value: Time.now.strftime("%H:%M"), class: "form-input" %>
    </div>

    <div class="form-group">
    <%= form.label :Recipe, style: "display: block" %>
    <%= form.file_field :featured_image, accept: "image/*" %>
    </div>

    <div class="form-group">
      <%= form.label :amount, class: "form-label" %>
      <%= form.text_field :amount, class: "form-input" %>
    </div>

    <div class="form-group">
      <%= form.submit "Submit", class: "form-submit" %>
    </div>
  </div>
<% end %>
</body>
</html>
